import { StyleSheet } from "react-native"

export const global = StyleSheet.create({
  title: {
    letterSpacing: .5,
    fontSize: 38,
    textAlign: 'center',
    textTransform: 'uppercase',
    fontWeight: '700'
  }
})